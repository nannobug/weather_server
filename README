1) Config the server:
    - Rename config.TEMPLATE.js to config.js
    - Paste token into config.js
    - Token is shared via email

2) Initialize and start the server:
    npm install
    npm run start

3) Visit localhost:3000/local/<zipcode>
    Current server only supports valid US zip codes

4) Test
    - Make sure port 3000 is free before running tests
    npm run test
      